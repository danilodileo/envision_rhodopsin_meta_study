# ENVISION — Rhodopsin metatranscriptomic study

Analysis code and figures for a metatranscriptomic study of microbial rhodopsin
expression in a genomic context, based on samples collected off the coast of
northeast Spain (ENVISION dataset). This repository accompanies the
manuscript currently in preparation / under review.

## Repository structure

```
.
├── scripts/    Quarto (.qmd) analysis scripts — see "Scripts" below
├── figures/    Final manuscript figures (PDF), regenerated by the scripts
└── README.md
```

## Data availability

The intermediate summary tables and metadata used as input to these scripts
are **not included** in this repository: the manuscript is not yet published,
and the underlying tables are kept private until then. The analysis **code**
is fully open here so reviewers and readers can inspect exactly how each
figure was produced.

If you need access to the input tables (e.g. for peer review), please contact
**Danilo Di Leo** — danilo.dileo@hotmail.com.

## Requirements

- **R** ≥ 4.5 and **[Quarto](https://quarto.org/)** ≥ 1.3 (developed with R 4.5.2 / Quarto 1.3.450)
- Internet access when rendering: some chunks query live services (NOAA
  bathymetry via `marmap`, KEGG pathway/module annotation via `clusterProfiler`
  / `KEGGREST`, `bitr_kegg()`)

### R packages

Most packages are on CRAN; a few are on Bioconductor.

```r
install.packages(c(
  "tidyverse", "readxl", "readr", "dplyr", "tidyr", "purrr", "stringr",
  "forcats", "lubridate", "tibble", "reshape2",
  "ggplot2", "ggrepel", "ggsignif", "ggvenn", "ggpmisc", "ggpubr", "ggtext",
  "ggplotify", "shadowtext", "patchwork", "viridis",
  "pheatmap", "corrr", "ggcorrplot", "circlize",
  "vegan", "FactoMineR", "factoextra", "missMDA", "compositions", "ineq",
  "broom", "gt", "gridExtra", "grid", "kableExtra", "DT", "reactable", "plotly",
  "sf", "rnaturalearth", "rnaturalearthdata", "marmap", "leaflet", "ggmap",
  "shiny", "kfigr", "grateful", "igraph", "Hmisc"
))

if (!requireNamespace("BiocManager", quietly = TRUE)) install.packages("BiocManager")
BiocManager::install(c("ComplexHeatmap", "clusterProfiler", "pathview", "KEGGREST"))
```

## Scripts

All scripts are self-contained Quarto documents (`format: html`) with
`cache: true` set globally, so repeated renders reuse unchanged chunks. Render
any of them with:

```bash
quarto render scripts/<script-name>.qmd
```

Each script expects to be run from a working directory containing the
(private) `data/` and `summary_tables/` folders described above, and writes
its figures to a local `figures/` subfolder.

| Script | What it does |
|---|---|
| `prepare_tables.qmd` | Data-preparation pipeline. Reads the raw taxonomy, KEGG/eggNOG annotation and count outputs and writes the intermediate summary tables (`summary_tables/*.tsv.gz`) and cleaned metadata (`data/envision_metadata.tsv.gz`) used by every other script. **Cannot be re-run without the raw inputs**, which are not distributed here; included for pipeline transparency. |
| `Overall_community_composition_and_environmental_variable_analysis.qmd` | Site/season overview: sampling map with an Atlantic/Europe locator inset, environmental variable time series, mapping-rate statistics, rhodopsin-expressing genome overlap, taxonomic composition, NMDS ordination of community structure, and family-level rhodopsin expression heatmaps against environmental variables. Produces **Figure 1** and **Figure 2**. |
| `fine-scale-analysis.qmd` | Genome-resolved analysis of the top-30 rhodopsin-expressing genomes: multi-track ComplexHeatmap of genome abundance/rhodopsin expression, Spearman correlations between rhodopsin and whole-genome transcription (per genome and per family), and family-specific time-series of rhodopsin expression vs. environmental variables (Poseidoniaceae, Puniceispirillaceae, Porticoccaceae) and KO-level bubble plots (Pelagibacteraceae, Porticoccaceae). Produces **Figure 3**, **Figure 4**, **Figure S4**, **Figure S6**, **Figure S7**, **Figure S8**, **Figure S9**. |
| `untargeted_analysis.qmd` | KEGG pathway/module enrichment analysis (family- and genome-level GSEA), motility/pili/transporter KO correlation heatmaps, and rhodopsin-transporter co-expression in *Pelagibacter* genomes. Produces **Figure 5**. |

### Figure → script map

| Figure | Script | Chunk |
|---|---|---|
| Figure 1 | `Overall_community_composition_and_environmental_variable_analysis.qmd` | `fig1-map-envision-sites` → `Environmental-variables` |
| Figure 2 | `Overall_community_composition_and_environmental_variable_analysis.qmd` | `fig3-rda` |
| Figure 3 | `fine-scale-analysis.qmd` | `spearman-genome-prd-env` → `plot-fig3` |
| Figure 4 | `fine-scale-analysis.qmd` | `heatmap-code-preparation` → `heatmaps-based-on-top-30-genomes-expressing-rhodopsin` (saved as `fig4.png` in the run directory — move/rename into `figures/`) |
| Figure 5 | `untargeted_analysis.qmd` | `try-grouping-transporters` → `correlation-plot-pelagibacter-transporters` → `fig-5` |
| Figure S4 | `fine-scale-analysis.qmd` | `correlation-rhodopsin-within-genome-and-genome-transcription-level-by-genome` |
| Figure S6 | `fine-scale-analysis.qmd` | `supplementary-figure-6` |
| Figure S7 | `fine-scale-analysis.qmd` | `supplementary-figure-7` |
| Figure S8 | `fine-scale-analysis.qmd` | `pela-bubble-visualization` |
| Figure S9 | `fine-scale-analysis.qmd` | `porti-bubble-visualization` |

Every figure-producing chunk saves both a vector PDF and a 300 dpi PNG to
`figures/` via `ggsave()` (PDF figures use `device = cairo_pdf` where genome
or KO labels contain non-ASCII characters, to avoid font-encoding artifacts).

## Citation

If you use this code, please cite the associated manuscript (citation to be
added upon publication).
